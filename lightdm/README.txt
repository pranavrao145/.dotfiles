This directory contains configuration files for the LightDM display manager.
They should go into /etc/lightdm. I do not recommend symlinking these, because,
as I learned the hard way, this breaks lightdm. Simply copy these files to the 
correct directory.
